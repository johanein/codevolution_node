// const add = (a, b) => {
//   return a + b;
// };

// export default add;

// 2)

// export default (a, b) => {
//   return a + b;
// };

// 3)

const add = (a, b) => {
  return a + b;
};

const subtract = (a, b) => {
  return a - b;
};

// export default {
//   add,
//   subtract,
// };
export { add, subtract };
